<script>
  window.lp.tracking = {
    pageName: "jobs : " + "{{page.title}}",
    channel: "jobs",
    evar42: "jobs"
  };
  window.lp.analytics.dataLayer.summaryTag = {
    context: "jobs",
    content_topic: "{{page.title}}:jobs",
    content_type: "landing page",
    content_id: "jobs"
  };
</script>
